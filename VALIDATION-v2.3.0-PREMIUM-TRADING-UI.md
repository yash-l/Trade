# HYDRA Ω-APEX v2.3.0 — Premium Trading UI Validation

## Verified locally
- 28 pytest tests: PASS
- Python compileall: PASS
- JavaScript syntax (`node --check dashboard/app.js`): PASS
- UI mapping verifier: PASS
- DOM-reference verifier: PASS
- Server startup: PASS
- `/api/health`: HTTP 200
- `/api/status`: HTTP 200
- `/api/settings`: HTTP 200
- `/api/settings/save`: PASS in isolated test copy; restart required by design
- `/api/export/trades.csv`: HTTP 200
- `/api/export/events.csv`: HTTP 200
- `/api/export/training.csv`: HTTP 200
- UI build marker: `2.3.0-premium-paper`
- LIVE remains fail-closed by default

## UI surface
- Overview
- APEX Core
- Trades
- Adaptive Learning
- Broker
- Research
- System
- Settings
- More
- Mobile bottom navigation: Home / APEX / Trades / Adaptive / More

## Mapped actions
- Refresh
- Export trades
- Export events
- Export training dataset
- 5paisa OAuth URL
- Read-only 5paisa data check
- 5paisa reconciliation
- Research audit instruction
- Settings save

## Safety
- Hard risk, daily loss, max trades and live enable/compliance controls are locked in UI.
- Adaptive trailing remains shadow-only by default.
- Settings writes are whitelist-limited and require a controlled restart.
- No synthetic trades are generated by the UI.

## Not claimed
This release does not claim real NSE market-hours validation, real 5paisa credential validation, regulatory approval, profitability, or live-money readiness. Those require external evidence.
